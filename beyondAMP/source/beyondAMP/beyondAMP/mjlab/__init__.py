"""mjlab backend for beyondAMP.

Mirrors the layout of ``beyondAMP.isaaclab`` but targets mjlab's
``ManagerBasedRlEnv``. The two backends are independent — picking one
does not require the other to be installed.
"""
